// Manipulando Arrays

// Transformar uma cadeia de caracteres em elementos de um array

let word = "manipulação" // cadeia de caracteres (string)

console.log(Array.from(word)) // O proprio "Array" já em um objeto disponivel de maneira global, metodo from que espera como argumento uma string.