// Object

const person = {
    name: 'Gustavo',
    age: 30,
    weight: 88.6,
    isAdmin: true
}

// Aparecera o objeto completo
console.log(person)

// Aparecera somente o valor da propriedade (name ou age)
console.log(person.name)

console.log(`${person.name} tem ${person.age} anos`)
